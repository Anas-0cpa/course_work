# 🎂 Birthday Reminder App (Python + Tkinter)

A simple yet elegant desktop application to manage and remind upcoming birthdays, built using Python and the `tkinter` GUI toolkit. The app supports adding, storing, and displaying reminders for birthdays, and notifies users when a birthday is today or within the next 7 days.

## 🧠 Features

- Add new birthday entries (Name, Date of Birth, Contact)
- View automatic birthday reminders
- Local storage in `birthdays.json`
- GUI-based interface using `tkinter`
- Object-Oriented Programming with Abstraction, Encapsulation, and Inheritance
- Automatically loads sample data if no file is found

## 🏗️ Technologies Used

- **Python 3**
- **tkinter** (for GUI)
- **json** (for saving birthday data)
- **abc** (abstract base classes for OOP)
- **datetime** (for birthday calculations)
- **os** (for file existence checking)

## 📦 Installation

1. **Clone this repository** (or copy the code):

```bash
git clone https://github.com/your-username/birthday-reminder-app.git
cd birthday-reminder-app
